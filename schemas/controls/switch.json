{
    "$id": "#/definitions/switch",
    "type": "object",
    "required": [
        "type",
        "options",
        "currentIndex"
    ],
    "properties": {
        "type": {
            "const": "switch"
        },
        "options": {
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "currentIndex": {
            "type": "number"
        },
        "image": {
            "type": "string",
            "format": "uri",
            "description": "URI to the image representing this specific switch's visual appearance"
        }
    }
}